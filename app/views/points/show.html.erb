<table>
  <tr>
    <td>id</td>
    <td>point_name</td>
    <td>register date</td>
  </tr>
<% @points.each do |point| %>
  <tr>
    <td><%= "#{point.id}" %></td>
    <td><%= "#{point.name}" %></td>
    <td><%= "#{point.created_at}" %></td>
  </tr>
<% end %>
</table>
